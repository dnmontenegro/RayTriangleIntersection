from vector3d import Vector3d

class Transformation3d:
   _translation: Vector3d
   _transformation: 
   _inverse_transformation:

    def __init__(self, translation: Vector3d, transformation, inverse_transformation):
        self._origin = origin
        self._direction = normalize(direction)